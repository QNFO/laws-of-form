# Bayesian Evidential Weight Gate (KIF-60) — QNFO.SLB.002 void-is-not-false

**Phase 4 (P4) gate artifact** | **Date:** 2026-08-12
**Applies to:** every cross-domain / structural correspondence claim in this project.

---

## 1. Pre-Registration Record

Timestamped predictions (this session, git-committed in `PROJECT-PLAN.md` §2 and this artifact):

- **P1 (locked P6):** "The unmarked state (void) is not a truth value; `false` is itself a mark; Boolean void ≡ false is a category error." — committed 2026-08-12, branch `slb/paper/void-is-not-false`, commit `63f5ed6`.
- **P2 (P4 sketch, Conjecture V2):** "The distinction-logic semantics (inside/outside/oscillating) is genuinely different from intuitionistic and paraconsistent semantics." — committed 2026-08-12 in `docs/deep-research.md`.

## 2. Falsifiability Matrix

| Claim | Disconfirmation condition |
|---|---|
| P1 (void ≠ false) | A formal account treats `false` as genuinely unmarked without losing Boolean theorems (reification eliminable), OR the re-entrant calculus cannot reproduce the valid classical fragment |
| P2 (V2: genuine discrimination) | The oscillating state reduces to a static paraconsistent dialetheia or a static trivalent (neutrosophic) value — i.e., no formula distinguishes the semantics |
| Classical-fragment recovery | Boolean logic is NOT the static restriction of distinction-logic |

## 3. Surprise Accounting

For the correspondence "void (unmarked) ↔ not-false; negation = crossing":

- **Null model:** the claim that "the void is not false" could be a re-labeling of the standard empty-type / empty-set distinction. Prior estimate: $P(\text{match} \mid \text{random})$ is HIGH — the critique risks being a terminological variant.
- **However:** the *logical consequence* claim (excluded middle is local; re-entry produces a dynamic state) is not trivial: static three-valued logics do not generally exhibit oscillation as a *semantic* state.
- **Net:** $\Delta \log\text{-odds}$ for the critique is modestly > 0; for the reconstruction (V1/V2) it is ~0 until the discrimination formula is exhibited.

## 4. Δlog-odds Summary

| Claim | Δlog-odds | Status |
|---|---|---|
| P1 (void ≠ false, as a critique) | > 0 (modest) — a defensible observation independent of construction | Candidate positive weight for the *critique* |
| P1 (as a new semantics) | ~0 until formalized | `[NOT YET EVIDENCE]` |
| P2 (V2: discrimination from paraconsistent/intuitionistic) | ~0 (no discrimination formula yet) | `[RETRODICTION — not evidence]` until exhibited |

## 5. Trap Audit

| Trap | Check |
|---|---|
| Overfitting | The framework can label any third state "oscillating". Mitigation: the confirmation-seeking test requires a formula whose distinction-logic value differs from BOTH intuitionistic and paraconsistent assignments — a constrained target. |
| Cherry-picking | Denominator: intuitionistic, paraconsistent, AND neutrosophic/trivalent are all named as alternatives (Phase 2 KIF-18). No cherry-picking. |
| Absorption | Risk is HIGH: "every counterexample is a new oscillation mode" would be absorption. Pre-declare the allowed oscillation modes before examining counterexamples (P4 open item). |

## 6. Gate Output

**Verdict: BLOCK the correspondence claims as evidence until Conjecture V2 (the discrimination formula) is exhibited.** The critique portion (void ≠ false; negation = crossing) carries candidate positive weight and may proceed as a standalone claim. The reconstruction (distinction-logic with an oscillating state) is `[NOT YET EVIDENCE]` until the confirmation-seeking test passes. This gate is the P4 red-team target.
